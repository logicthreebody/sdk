配置环境变量
NODE_HOME=/opt/node-v16
PATH=$NODE_HOME/bin:$PATH


##淘宝镜像源最新地址
npm config set registry https://registry.npmmirror.com
#查看设置是否成功
npm get registry

###########
# 将 npm 升级到最新版
npm install npm -g
# 如果要指定版本，可以通过@指定版本
npm install -g npm@8.19.1
